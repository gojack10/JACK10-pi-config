diff --git a/packages/ai/src/models.ts b/packages/ai/src/models.ts
index dfc5202..f6f7c1c 100644
--- a/packages/ai/src/models.ts
+++ b/packages/ai/src/models.ts
@@ -97,6 +97,7 @@ export type ModelsDeferredCancelOptions = DeferredCancelOptions & ModelsRequestT
 export interface Provider<TApi extends Api = Api> {
 	readonly id: string;
 	readonly name: string;
+	readonly selectable?: boolean;
 
 	readonly baseUrl?: string;
 	readonly headers?: ProviderHeaders;
@@ -527,7 +528,7 @@ class ModelsImpl implements MutableModels {
 				? [this.providers.get(providerId)].filter((entry) => entry !== undefined)
 				: this.getProviders();
 			const checks = await Promise.all(
-				providers.map(async (provider) => {
+				providers.filter((provider) => provider.selectable !== false).map(async (provider) => {
 					const credential = await this.readCredential(provider.id, signal);
 					return { provider, credential, auth: await this.checkProviderAuth(provider, credential, signal) };
 				}),
diff --git a/packages/ai/test/models-runtime.test.ts b/packages/ai/test/models-runtime.test.ts
index ffbcf6a..ea3235d 100644
--- a/packages/ai/test/models-runtime.test.ts
+++ b/packages/ai/test/models-runtime.test.ts
@@ -59,6 +59,7 @@ function testProvider(input: {
 	getModels?: () => readonly Model<Api>[];
 	refreshModels?: Provider["refreshModels"];
 	calls?: ProviderCall[];
+	selectable?: boolean;
 }): Provider {
 	const models = input.models ?? [testModel(input.id, "model-a")];
 	const respond = (model: Model<Api>, options: StreamOptions | undefined) => {
@@ -73,6 +74,7 @@ function testProvider(input: {
 	return {
 		id: input.id,
 		name: input.id,
+		selectable: input.selectable,
 		auth: input.auth ?? { apiKey: ambientAuth },
 		getModels: input.getModels ?? (() => models),
 		refreshModels: input.refreshModels,
@@ -809,6 +811,7 @@ describe("Models runtime", () => {
 		const models = createModels({ credentials });
 		models.setProvider(testProvider({ id: "ambient", auth: { apiKey: envKeyAuth("env-key") } }));
 		models.setProvider(testProvider({ id: "missing", auth: { apiKey: envKeyAuth(undefined) } }));
+		models.setProvider(testProvider({ id: "hidden", selectable: false }));
 		models.setProvider(
 			testProvider({
 				id: "oauth",
@@ -835,6 +838,8 @@ describe("Models runtime", () => {
 		expect(refreshes).toBe(0);
 		expect((await models.getAvailable()).map((model) => model.provider)).toEqual(["ambient", "oauth"]);
 		expect((await models.getAvailable("ambient")).map((model) => model.provider)).toEqual(["ambient"]);
+		expect(await models.getAvailable("hidden")).toEqual([]);
+		expect(await models.checkAuth("hidden")).toMatchObject({ type: "api_key" });
 	});
 
 	it("runs provider login and logout through the credential store", async () => {
diff --git a/packages/coding-agent/src/core/model-runtime.ts b/packages/coding-agent/src/core/model-runtime.ts
index 0ecd88d..3b1ff52 100644
--- a/packages/coding-agent/src/core/model-runtime.ts
+++ b/packages/coding-agent/src/core/model-runtime.ts
@@ -274,7 +274,11 @@ export class ModelRuntime implements Models {
 		this.snapshot = {
 			...this.snapshot,
 			all,
-			available: all.filter((model) => this.snapshot.configuredProviders.has(model.provider)),
+			available: all.filter(
+				(model) =>
+					this.snapshot.configuredProviders.has(model.provider) &&
+					this.models.getProvider(model.provider)?.selectable !== false,
+			),
 		};
 	}
 
diff --git a/packages/coding-agent/src/modes/interactive/interactive-mode.ts b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
index ef6d996..b3ae3e9 100644
--- a/packages/coding-agent/src/modes/interactive/interactive-mode.ts
+++ b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
@@ -5157,6 +5157,7 @@ export class InteractiveMode {
 	private getLoginProviderOptions(authType?: "oauth" | "api_key"): AuthSelectorProvider[] {
 		const options: AuthSelectorProvider[] = [];
 		for (const provider of this.session.modelRuntime.getProviders()) {
+			if (provider.selectable === false) continue;
 			const authStatus = this.session.modelRuntime.getProviderAuthStatus(provider.id);
 			const status = authStatus.configured
 				? {
diff --git a/packages/coding-agent/test/oauth-selector.test.ts b/packages/coding-agent/test/oauth-selector.test.ts
index 71cfb2f..6f78dca 100644
--- a/packages/coding-agent/test/oauth-selector.test.ts
+++ b/packages/coding-agent/test/oauth-selector.test.ts
@@ -40,6 +40,12 @@ describe("OAuthSelectorComponent", () => {
 				name: "Google Vertex AI",
 				auth: { apiKey: { name: "Google Cloud credentials" } },
 			},
+			{
+				id: "hidden-account",
+				name: "Hidden account",
+				selectable: false,
+				auth: { oauth: { name: "Hidden OAuth", login: async () => ({}) } },
+			},
 		];
 		const fakeThis = {
 			session: {
@@ -52,6 +58,7 @@ describe("OAuthSelectorComponent", () => {
 		};
 
 		const apiKeyOptions = getLoginProviderOptions.call(fakeThis, "api_key");
+		expect(getLoginProviderOptions.call(fakeThis).some((option) => option.id === "hidden-account")).toBe(false);
 		expect(apiKeyOptions).toMatchObject([
 			{
 				id: "anthropic",
